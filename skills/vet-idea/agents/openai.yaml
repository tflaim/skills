interface:
  display_name: "Vet Idea"
  short_description: "Stress-test an idea before building it"
  default_prompt: "Use $vet-idea to challenge this idea and identify the decisions needed before implementation."
policy:
  allow_implicit_invocation: false
